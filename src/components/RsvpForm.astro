---
interface Props {
  formAction: string;
  nameField: string;
  attendingField: string;
  guestsField: string;
  phoneField: string;
}

const { formAction, nameField, attendingField, guestsField, phoneField } =
  Astro.props;
---

<h2 class="rsvp-heading">RSVP</h2>
<p class="rsvp-subtext">Kindly let us know if you'll be joining us</p>
<form
  class="rsvp-form"
  id="rsvpForm"
  method="POST"
  action={formAction}
  target="hiddenRsvpFrame"
>
  <div class="rsvp-field">
    <label for="rsvpName">Full Name</label>
    <input type="text" id="rsvpName" name={nameField} required />
  </div>
  <div class="rsvp-field">
    <label for="rsvpAttending">Are you attending?</label>
    <select id="rsvpAttending" name={attendingField} required>
      <option value="" disabled selected>Select an option</option>
      <option value="Yes">Yes, I'll be there</option>
      <option value="No">Sorry, can't make it</option>
    </select>
  </div>
  <div class="rsvp-field">
    <label for="rsvpGuests">Number of Guests (incl. you)</label>
    <select id="rsvpGuests" name={guestsField} required>
      <option value="1">1</option>
      <option value="2">2</option>
      <option value="3">3</option>
      <option value="4">4+</option>
    </select>
  </div>
  <div class="rsvp-field">
    <label for="rsvpPhone">Contact Number</label>
    <input type="tel" id="rsvpPhone" name={phoneField} required />
  </div>
  <button type="submit" class="rsvp-submit">Send RSVP</button>
</form>
<p class="rsvp-success" id="rsvpSuccess">
  Thank you! Your RSVP has been received.
</p>
<iframe
  name="hiddenRsvpFrame"
  id="hiddenRsvpFrame"
  title="RSVP submission (hidden)"
  style="display:none"
></iframe>
